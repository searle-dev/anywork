apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: anywork

resources:
  - namespace.yaml
  - rbac.yaml
  - server-deployment.yaml
  # worker-pod-template.yaml is a reference template; not applied directly
